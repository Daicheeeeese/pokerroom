{
  "buildCommand": "npx prisma generate --schema=./prisma/schema.prisma && npx prisma migrate reset --force --accept-data-loss --schema=./prisma/schema.prisma && next build",
  "installCommand": "npm install",
  "framework": "nextjs"
} 