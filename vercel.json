{
  "buildCommand": "npx prisma generate --schema=./prisma/schema.prisma && npx prisma migrate reset --force --schema=./prisma/schema.prisma && npx prisma db seed && next build",
  "installCommand": "npm install",
  "framework": "nextjs"
} 