{
  "buildCommand": "npx prisma generate && npx prisma migrate reset --force --accept-data-loss --schema=./prisma/schema.prisma && next build",
  "installCommand": "npm install",
  "framework": "nextjs"
} 